@using ComputerShop.Models
@model Product
@{ 
    
}
<tr>
    @using (Html.BeginForm("Create", "Product", FormMethod.Post, new { id = "createProduct", @class = "navbar-right" }))
    {
        <th>

            <input type="submit" class="btn btn-success" id="add-btn" value="Add" />
        </th>
        <th>
            @Html.TextBoxFor(m => m.product_type, new { @class = "form-control", @placeholder = "Product type" })
        </th>
        <th>
            @Html.TextBoxFor(m => m.production_date, new { @class = "form-control", @placeholder = "Product date" })
        </th>
        <th></th>
        <th></th>
    }
</tr>


